import { NextFunction, Request, Response } from "express"
import { ErrorHandle } from "../../error/error";

const CATEGORY_GET = (req: Request, res: Response, next:NextFunction) => {
 try {
    
 } catch (error) {
    console.log(error);
    
 }


}

export {CATEGORY_GET}




